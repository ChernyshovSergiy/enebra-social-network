export class Referens {
    id: number;
    title: string;
    icon: string;
    name: string;

    constructor(id: number, title: string, icon: string, name: string) {
        this.id = id;
        this.title = title;
        this.icon = icon;
        this.name = name;
    }
}
