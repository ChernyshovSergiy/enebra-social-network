export class Point {
    icon: string;
    name: object;
    link: string;

    constructor(icon: string, name: object, link: string) {
        this.icon = icon;
        this.name = name;
        this.link = link;
    }
}
